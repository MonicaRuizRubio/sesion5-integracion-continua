/* Función suma */
var suma = function suma (a, b) {
	return a + b;	
};

/* Función resta */
var resta = function suma (a, b) {
	return a - b;	
};
/*Función esPar */
 function esPar(numero) {
  return numero % 2 === 0;
};


/* Exportación de funciones */
module.exports = {
	esPar
}

